# Graphics Playground

Retired attempt of building a wrapper lib around vulkan until I realized it's not really a good idea to abstract most things.
Some exceptions where it seemed to work well was handling of instance, device and swapchain but VkBootstrap exists so what's the point...
